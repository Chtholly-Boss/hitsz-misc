#import "@preview/gentle-clues:1.0.0" as clues

// Alpha, Beta, Gamma,...
#let alpha = sym.alpha
#let beta = sym.beta
#let gamma = sym.gamma
#let delta = sym.delta
#let epsilon = sym.epsilon
// Arrows
#let to = sym.arrow.r
#let dto = sym.arrow.double
// Miscelaneous
#let dot = sym.dot
