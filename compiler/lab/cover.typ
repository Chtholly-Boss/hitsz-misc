#align(center, block(
  height: 20%,
)[
  #text(size: 20pt)[
    哈尔滨工业大学(深圳)
  ]
])

#align(center, block(
  height: 20%,
)[
  #text(
  size: 40pt,
  font: "STZhongsong", // 原文档为 宋体
  weight: "bold"
  )[
    #underline(offset: 10pt)[
      《编译原理》实验报告
    ]
  ]
])

#block(height: 10%)[]

#align(center, block(
  height: 25%,
)[
  #text(size: 20pt)[
    #table(
      columns: 2,
      stroke: none, 
      inset: 10pt,

      [学院：], [计算机科学与技术学院],
      table.hline(start: 1),
      [姓名：], [马奕斌],
      table.hline(start: 1),
      [学号：], [220110915],
      table.hline(start: 1),
      [专业：], [计算机科学与技术],
      table.hline(start: 1),
      [日期：], [2024/11/10],
      table.hline(start: 1),
    )
  ]
])
)

#pagebreak()